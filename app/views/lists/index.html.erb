<h1>リスト一覧</h1>
<div>
  <%= form_with(model: @list, local: true) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.submit "作成" %>
  <% end %>
</div>
<div>
  <% @lists.each do |list| %>
    <li><%= link_to list.title, list_path(list) %></li>
  <% end %>
</div>

